
@{
    ViewBag.Title = "Login";
    Layout = "~/Views/Shared/_LayoutContainer.cshtml";
}

<div class="login-form-wrapper container-wrapper">
    <div class="form-signin rounded-sm shadow" action="/Account/LoginAction">
        <!-- Nav tabs -->
        <div>
            <h3 class="text-center mb-4">ĐĂNG NHẬP</h3>
        </div>
        <!-- Tab panes -->
        <div class="tab-content">
            <!-- Tab1 -->

            <div id="login" class="container tab-pane active">
                <form id="loginForm" action="~/account/LoginAction" method="post">
                    <label for="loginEmail" class="sr-only">UserName</label>
                    <input name="username" type="text" id="username" class="form-control mb-4" placeholder="username" autofocus />
                    <label for="loginPassword" class="sr-only">Password</label>
                    <input name="password" type="password" id="password" class="form-control mb-4" placeholder="password" />
                    <div class="one-line mb-2">
                        <div class="checkbox">
                            <label>
                                <input name="remember" type="checkbox" value="remember-me" />Lưu mật khẩu
                            </label>
                        </div>

                    </div>

                    <div class="d-flex justify-content-center mb-4">
                        <button class="btn btn-lg btn-login " type="submit">Đăng nhập</button>
                    </div>
                    <div class="d-flex justify-content-center">
                        <a class="forgot me-2" href="#!">Quên mật khẩu?</a>
                        <a href="~/account/register">Create a account</a>
                    </div>
                    <div class="form-group">
                        <p class="text-danger">
                            @TempData["error"]
                        </p>

                    </div>
                    <p class="mt-4 mb-0 text-muted small">
                        Hãy đăng nhập ngay để khám phá thế giới thời trang tại cửa hàng
                        Skyline Shop.
                    </p>
                </form>
            </div>
        </div>
    </div>
</div>