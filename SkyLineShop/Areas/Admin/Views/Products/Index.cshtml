@model IEnumerable<SkyLineShop.Models.Product>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/AdminLayout.cshtml";
}
<div id="page-wrapper">
    <div class="container-fluid">
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">Quản lý sản phẩm</h1>
            </div>
            <!-- /.col-lg-12 -->
        </div>
        <!-- /.row -->
        <div class="row">
            <div class="col-lg-12">
                <div class="panel panel-default">
                    <div class="panel-heading">
                        Quản lý sản phẩm
                    </div>
                    <!-- /.panel-heading -->
                    <div class="panel-body">
                        <div class="table-responsive">
                            <p>
                                @Html.ActionLink("Create New", "Create")
                            </p>
                            <p>
                                @Html.ActionLink("SQL TO XML", "convert")
                            </p>
                            <table class="table table-striped table-bordered table-hover" id="dataTables-example">

                                <thead>
                                    <tr>
                                        <th>
                                            STT
                                        </th>

                                        <th>
                                            Product Name
                                        </th>
                                        <th>
                                            Price
                                        </th>
                                        <th>
                                            Description
                                        </th>
                                        <th>
                                            Brand
                                        </th>
                                        <th>
                                            Category
                                        </th>
                                        @*<th>
                        Image
                    </th>*@
                                        <th>#</th>
                                    </tr>
                                </thead>

                                @*
            @{int index = 0; }
            @foreach (var item in Model)
            {
                index++;
                <tr>
                    <td>
                        @index
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.product_name)
                    </td>
                    <td>

                        @String.Format("{0:0,0}", item.price)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.desc)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Brand.brand_name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Category.cate_name)
                    </td>

                    <td>
                        @Html.ActionLink("Edit", "Edit", new { id = item.id_product }) |
                        @Html.ActionLink("Details", "Details", new { id = item.id_product }) |
                        @Html.ActionLink("Delete", "Delete", new { id = item.id_product })
                    </td>
                </tr>
            }
                                *@

                                <tbody>
                                    @{
                                        int index = 0;
                                        foreach (var product in ViewBag.ProductXML.Descendants("Product"))
                                        {
                                            index++;
                                                                        <tr>
                                                                            @if (product != null)
                                                                            {

                                                                                <td>@index</td>
                                                                                <td>@product.Attribute("product_name").Value</td>
                                                                                <td>@String.Format("{0:0,0}", decimal.Parse(product.Attribute("price").Value))</td>
                                                                                <td>@product.Attribute("description").Value</td>
                                                                                <td>@product.Element("Brand").Attribute("brand_name").Value</td>
                                                                                <td>@product.Element("Category").Attribute("category_name").Value</td>

                                                                                <!-- Các cột khác -->
                                                                                <td>
                                                                                    <!-- Hiển thị nút hoặc hành động cho mỗi sản phẩm -->
                                                                                    @Html.ActionLink("Edit", "Edit", new { id = product.Attribute("id_product").Value }) |
                                                                                    @Html.ActionLink("Details", "Details", new { id = product.Attribute("id_product").Value }) |
                                                                                    @Html.ActionLink("Delete", "Delete", new { id = product.Attribute("id_product").Value })
                                                                                </td>
                                                                            }
                                                                        </tr>
                                        }
                                    }
                                </tbody>
                            </table>

                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>